{
  "name": "arcane-repo",
  "private": true,
  "packageManager": "pnpm@10.18.2",
  "workspaces": [
    "frontend",
    "tests"
  ],
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "pnpm -C arcane-frontend dev",
    "build": "pnpm -C arcane-frontend build",
    "test:up": "docker compose -f tests/setup/compose.yaml up -d --build",
    "test:down": "docker compose -f tests/setup/compose.yaml down",
    "test": "pnpm test:up && pnpm -C arcane-tests test && pnpm test:down",
    "format": "pnpm -C arcane-frontend format",
    "check": "pnpm -C arcane-frontend check"
  }
}
